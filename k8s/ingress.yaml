apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rag-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
    - host: ""          # on accepte tout, tu testes à l'IP
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: rag-frontend-service
                port:
                  number: 80
